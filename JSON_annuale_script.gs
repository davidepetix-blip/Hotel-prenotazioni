// DEPRECATO — vedi blip-appscript.gs (unica versione mantenuta).
// File svuotato il 2026-08-17: duplicava TUTTE le funzioni di blip-appscript.gs
// (doGet, doPost, onEdit, onOpen, estraiSegmenti, aggiornaJSONAnnuale, ecc.)
// con codice piu' vecchio, incluso un onEdit ancora col bug e.user che
// impediva la sync delle prenotazioni inserite a mano sul foglio. Con due
// file .gs nello stesso progetto Apps Script che dichiarano le stesse
// funzioni, quale versione vince dipende dall'ordine di caricamento interno
// di Apps Script: rischio reale di eseguire silenziosamente la versione
// sbagliata. File lasciato vuoto (anziche' cancellato) per problemi tecnici
// nell'interfaccia di cancellazione di GitHub in questa sessione — puo' essere
// rimosso completamente in un secondo momento.
